import express from'express';import{fileURLToPath}from'url';import path from'path';const app=express();const __dirname=path.dirname(fileURLToPath(import.meta.url));app.use(express.json());let data={siteName:'كل شيء بالمعقول',news:'تابعوا أحدث منشورات المحل',featured:'آخر المنشورات'};app.get('/api/health',(req,res)=>res.json({ok:true}));app.get('/api/settings',(req,res)=>res.json(data));app.post('/api/settings',(req,res)=>{data={...data,...req.body};res.json({ok:true,data})});app.get('/api/stats',(req,res)=>res.json({sales:0,products:0,customers:0,stock:0}));const dist=path.join(__dirname,'..','dist');app.use(express.static(dist));app.get('*',(req,res)=>res.sendFile(path.join(dist,'index.html')));const port=process.env.PORT||3000;app.listen(port,'0.0.0.0',()=>console.log(`belma3qoul-web running on ${port}`));